@model SpaceAgenciesDatabaseApp.CrewsAstronauts

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>CrewsAstronauts</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="CrewId" class="control-label"></label>
                <select asp-for="CrewId" class ="form-control" asp-items="ViewBag.CrewId"></select>
            </div>
            <div class="form-group">
                <label asp-for="AstronautId" class="control-label"></label>
                <select asp-for="AstronautId" class ="form-control" asp-items="ViewBag.AstronautId"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
